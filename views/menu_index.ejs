<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1>Welcome to gitpub</h1>
    <h2>Drinks</h2>
    <ul>
        <% for (let i = 0; i < drinks.length; i+=1){ %>
            <li><a href="menu/drinks/<%=i %>"><%=drinks[i].name.charAt(0).toUpperCase() + drinks[i].name.slice(1) %></a></li>

       <% } %>
    </ul>
    <h2>Food</h2>
    <ul>
        <% for (let i = 0; i < food.length; i+=1){ %>
            <li><a href="menu/food/<%=i %>"><%=food[i].name.charAt(0).toUpperCase() + food[i].name.slice(1) %></a></li>

       <% } %>
    </ul>

</body>
<style>
    body {
        color: blanchedalmond;
        background-color: steelblue;
    }
    
</style>
</html>